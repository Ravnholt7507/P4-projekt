package Types;

import expression.Expression;
import expression.Type;

public class String_type extends Expression {

    public String_type(Object result) {
        super(result);
    }

    public String_type(Type type) {
        super(type);
    }
}