package Types;

import expression.Expression;
import expression.Type;

public class Boolean_Type extends Expression {
	
	public Boolean_Type(Object result) {
		super(result);
	}
	
	public Boolean_Type(Type type) {
		super(type);
	}
}
