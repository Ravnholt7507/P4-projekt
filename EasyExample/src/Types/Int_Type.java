package Types;

import expression.Expression;
import expression.Type;

public class Int_Type extends Expression {
	
	public Int_Type(Object result) {
		super(result);
	}
	
	public Int_Type(Type type) {
		super(type);
	}
}
